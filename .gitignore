# Dependencies
node_modules/
.vercel/

# Environment variables (NEVER commit these)
.env
.env.local
.env.production
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Backups
*.backup
*.bak

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/

# Development/migration tools (should not be deployed)
dev-tools/data-migration.html
dev-tools/community_updated.html

# SQL schemas and dev docs in frontend (should not be public)
frontend/js/*.sql
frontend/js/*.md
frontend/js/*.txt

# Large data files (not for deployment)
PAP_Digital_Magazine/
pap-editorials-ALL.json
pap-editorials-full.json
pap-films-ALL.json
pap-youtube-shorts.json
scripts/output/*.json

design-mockups/
test_film_related_button.js

# Claude agent worktrees (local tooling)
# .claude: 공유 설정(rules/agents/commands/hooks/settings.json)은 추적, 로컬 전용만 무시
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/
.claude/agent-memory/
_to_delete/
*.command

# 2026-07-21 — 저장소에 들어가면 안 되는 로컬 부산물
# git lock 파일 격리 폴더 (로컬 전용) / Claude 데이터 내보내기 덤프
_gitlock_quarantine/
data-*-batch-*/
PAP_한국성장_데일리브리프_*.md
2026-*_PAP_한국성장_브리프.md

_patch_tmp_quarantine/
# Claude 작업용 임시 스크립트·로그 (커밋 대상 아님)
outputs_scratch/
.fuse_hidden*
_to_delete_gitlocks/
